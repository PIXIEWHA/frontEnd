import 'package:flutter/material.dart';
import 'package:front/Screens/Login/components/body.dart';

class VideoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
